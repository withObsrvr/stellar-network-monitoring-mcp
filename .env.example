# Stellar Network Configuration
STELLAR_NETWORK=mainnet  # or "testnet" for Stellar testnet
LOG_LEVEL=info           # debug, info, warn, error

# Example usage:
# cp .env.example .env
# Edit .env with your preferred settings
# The server will automatically load these settings